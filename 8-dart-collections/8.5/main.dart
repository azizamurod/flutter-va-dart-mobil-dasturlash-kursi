void main() {
  // // map
  // var numbers = [1, 2, 3, 4];
  // var doubled = numbers.map((el) => el * 2);

  // // where
  // var numbers = [1, 2, 3, 4, 5, 6];
  // var evenNumbers = numbers.where((el) => el % 2 == 0);

  // // reduce
  // var numbers = [1, 2, 3, 4];
  // var sum = numbers.reduce((initial, el) => initial + el);

  // forEach
  var fruits = ['olma', 'banan', 'anor'];
  fruits.forEach((el) => print(el));
}
