import 'package:flutter/material.dart';

class ActiveSearchScreen extends StatelessWidget {
  const ActiveSearchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Active Search Screen'),
      ),
    );
  }
}
