import 'package:flutter/material.dart';

class LastViewedScreen extends StatelessWidget {
  const LastViewedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Last Viewed Screen'),
      ),
    );
  }
}
